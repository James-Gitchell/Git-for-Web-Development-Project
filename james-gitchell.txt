Link to finish project https://codepen.io/wes-gitchell/pen/MWbjwyq?editors=1100

. What is Semantic HTML? 
         Html that describes meaning
    2. What is HTML used for? 
         To provide structure to a web page, application, or 
    3. What is an attribute and where do we put it? 
         An attribute goes inside of an element tag and can be changed to different variables 
    4. What is the h1 tag used for? How many times should I use it on a page?
         The h1 tag should only be used once and it proclaims the main theme
    5. Name two tags that have required attributes
         <a> <img> anchors and imgs
    6. What do we put in the head of our HTML document? metadata, title, links
    7. What is an id? 
         A specific selecter that should only be used once on an html page
    8. What elements can I add an id to? 
       h1-6,p,div,section, many of the semantic html
    9. How many times can I use the same id on a page? 1
    10. What is a class? A selector used to style groups of data
    11. What elements can I add a class to?many semantic html, like p,h1-6,div,... 
    12. How many times can I use the same class on a page? multiple
    13. How do I get my link to open in a new tab? add the target="_blank" attribute
    14. What is the alt attribute in the image tag used for?
           used to describe info such as a picture if it cannot be displayed
    15. How do I reference an id? with a #
    16. What is the difference between a section and a div
         divs are non-semantic and are used to separate and style groups of information
    17. What is CSS used for? styleing the html
    18. How to we select an element? Example - every h2 on the page h2{};
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
         classes can be used multiple times applied to style multiple elements and information while id is specific 
           and can only be used once
    20. How do we select classes in CSS? .classname{};
    21. How do we select a p element with a single class of “human””?.human{};
    22. What is a parent child selector? When would this be useful? to specifically style only the element-ex. "p"
        inside a div for example   div p{};
    23. How do you select all links within a div with the class of sidebar? .sidebar{};
    24. What is a pseudo selector? a keyword added to the selector to define a specifically 
         state  ex.    a:hover
    25. What do we use the change the spacing between lines? line-height
    26. What do we use to change the spacing between letters? letter-spacing
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform
    28. How do I add a 1px border around my div that is dotted and black?   border-style:dotted; Border-color; border-width;
    29. How do I select everything on the page? the universal selector *
    30. How do I write a comment in CSS? /* inform */
    31. How do I find out what file I am in, when I am using the command line? pwd - stands for print working directory
    32. Using the command line - how do I see a list of files/folders in my current folder? ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? rm -R command 
          be careful because you can and will remove subfolders and files
    34. Why should I use version control? to keep information and applications updated,relevant, etc..
    35. How often should I commit to github?every 20 min or after modifying your file
    36. What is the command we would use to push our repo up to github? git push -u origin branch-name
    37. Walk me through Lambda's git flow. 
         * First it is important to fork your own repository-- confirm 
         * Next you click on clone or download and copy that url
         * using my Git-bash (windows) type git clone + "url previously copied goes here"
         *cd into the repo just created by typing cd "repo-name"
         *create a branch for local editing, git checkout-b "your branch-name" ex. john-dotted
         *work on project adding and commiting often...  
              git add .   (ADDS TO STAGING AREA)
              git commit -m "this message should explain changes made
                 to benefit anyone including self when inheriting or viewing the codepen
              git push -u origin JOHN-DOE(branch name)- this will allow you to make a pull request later
                so that work can be collaborated on and put together in an efficient maner

Stretch Questions

    1. What is the difference between an inline element and a block element?
         * Block-level elements create a new line after the closing tag
         * inline elements do not... //!--not sure if block position up and 
             down and inline position left to right?--//
        I
    2. What happens when an element is positioned absolutely? 
          That element no longer exists on the normal doc layout flow<<<sorry 
             for the verbatum of the internet, still trying to understand<<<
               it appears to be on its own layer so that we can isolate ui features without interfering
                with the layout of other elements
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
